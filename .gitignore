# macOS
.DS_Store

# Node — the sample repo has no dependencies, but a reviewer may run npm
node_modules/
npm-debug.log*

# Nothing else belongs here. Working documents (TEST_PLAN.md, INTERVIEW_PREP.md)
# and captured baseline transcripts live outside this repository by design —
# see "Before and after" in README.md.
